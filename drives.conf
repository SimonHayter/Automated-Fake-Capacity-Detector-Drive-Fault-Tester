# Drives to test. Comma-separated, no quotes.
# Use the device name as shown by lsblk or GSmartControl (without /dev/).
#   SATA / USB drives : sda, sdb, sdc, ...
#   NVMe drives       : nvme0n1, nvme1n1, ...
#   SD / eMMC         : mmcblk0, mmcblk1, ...
DRIVES=(sda, sdb, sdc)
